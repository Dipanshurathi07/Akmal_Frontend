{
  "name": "akmal-frontend",
  "private": true,
  "scripts": {
    "build": "cd Turmeric_WholeSale && npm install && npm run build",
    "dev": "cd Turmeric_WholeSale && npm run dev",
    "lint": "cd Turmeric_WholeSale && npm run lint"
  }
}
